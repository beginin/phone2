<%= form_for(@typeconnect) do |f| %>
  <% if @typeconnect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typeconnect.errors.count, "error") %> prohibited this typeconnect from being saved:</h2>

      <ul>
      <% @typeconnect.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
