<%= form_for(@typecall) do |f| %>
  <% if @typecall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typecall.errors.count, "error") %> prohibited this typecall from being saved:</h2>

      <ul>
      <% @typecall.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
