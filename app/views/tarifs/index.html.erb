<h1>Тарифные планы</h1>

<table>
  <tr>
    <th>Наименование</th>
    <th>Описание</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tarifs.each do |tarif| %>
  <tr>
    <td><%= link_to tarif.name, tarif %></td>
    <td><%= tarif.description %></td>
    <td><%= link_to 'Редактировать', edit_tarif_path(tarif) %></td>
    <td><%= link_to 'Удалить', tarif, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tarif', new_tarif_path %>
