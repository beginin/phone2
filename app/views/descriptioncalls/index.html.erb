<h1>Описание звонка</h1>

<table>
  <tr>
    <th>Наименование</th>
    <th>Статус</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @descriptioncalls.each do |descriptioncall| %>
  <tr>
    <td><%= descriptioncall.name %></td>
    <td><%= descriptioncall.fin == true ? "Исключен" : "Включен" %></td>
    <td><%= link_to 'Show', descriptioncall %></td>
    <td><%= link_to 'Edit', edit_descriptioncall_path(descriptioncall) %></td>
    <td><%= link_to 'Destroy', descriptioncall, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новое описание звонка', new_descriptioncall_path %>
